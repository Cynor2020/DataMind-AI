# 💫 About Me:
Bhai, yahaan pura folder structure ke saath code de raha hoon. Har file ka exact location bata raha hoon, taaki tujhe samajhne me asani ho.<br><br><br>---<br><br>📂 DataMind-AI (Project Root)<br><br>DataMind-AI/<br>│── frontend/  # Next.js (Frontend)<br>│   ├── pages/<br>│   │   ├── login.js        # Login Page UI<br>│   │   ├── register.js     # Registration Page UI<br>│   ├── utils/<br>│   │   ├── auth.js         # Token management<br>│   ├── styles/             # Tailwind CSS styles<br>│   ├── package.json        # Dependencies<br>│── backend/  # FastAPI (Backend)<br>│   ├── routes/<br>│   │   ├── auth.py         # Login/Register API<br>│   ├── models/<br>│   │   ├── user_model.py   # User Schema<br>│   ├── main.py             # FastAPI App<br>│   ├── .env                # Environment Variables<br>│   ├── requirements.txt    # Dependencies<br><br><br>---<br><br>📂 Backend (FastAPI + MongoDB)<br><br>1️⃣ Install Dependencies<br><br>Backend me dependencies install karne ke liye:<br><br>cd backend<br>pip install fastapi uvicorn pymongo bcrypt python-dotenv pyjwt passlib<br><br><br>---<br><br>2️⃣ main.py (Backend Entry Point)<br><br>📍 Location: backend/main.py<br><br>from fastapi import FastAPI<br>from routes.auth import router as auth_router<br><br>app = FastAPI()<br><br># Register routes<br>app.include_router(auth_router, prefix="/auth", tags=["Authentication"])<br><br>@app.get("/")<br>def home():<br>    return {"message": "Welcome to DataMind AI"}<br><br><br>---<br><br>3️⃣ user_model.py (User Schema)<br><br>📍 Location: backend/models/user_model.py<br><br>from pydantic import BaseModel<br><br>class UserSchema(BaseModel):<br>    email: str<br>    password: str<br><br><br>---<br><br>4️⃣ auth.py (Authentication API)<br><br>📍 Location: backend/routes/auth.py<br><br>from fastapi import APIRouter, HTTPException<br>from models.user_model import UserSchema<br>from pymongo import MongoClient<br>import bcrypt, jwt, os<br><br>router = APIRouter()<br>client = MongoClient("mongodb://localhost:27017/")<br>db = client["datamind_ai"]<br>users = db["users"]<br><br>SECRET_KEY = os.getenv("SECRET_KEY", "mysecret")<br><br>@router.post("/register")<br>def register(user: UserSchema):<br>    if users.find_one({"email": user.email}):<br>        raise HTTPException(status_code=400, detail="User already exists")<br>    hashed_pw = bcrypt.hashpw(user.password.encode(), bcrypt.gensalt())<br>    users.insert_one({"email": user.email, "password": hashed_pw})<br>    return {"message": "User registered successfully"}<br><br>@router.post("/login")<br>def login(user: UserSchema):<br>    db_user = users.find_one({"email": user.email})<br>    if not db_user or not bcrypt.checkpw(user.password.encode(), db_user["password"]):<br>        raise HTTPException(status_code=401, detail="Invalid credentials")<br>    token = jwt.encode({"email": user.email}, SECRET_KEY, algorithm="HS256")<br>    return {"access_token": token}<br><br><br>---<br><br>📂 Frontend (Next.js + Tailwind CSS)<br><br>1️⃣ Install Dependencies<br><br>cd frontend<br>npm install next react react-dom axios tailwindcss<br><br><br>---<br><br>2️⃣ Configure Tailwind CSS<br><br>npx tailwindcss init<br><br><br>---<br><br>3️⃣ login.js (Login Page)<br><br>📍 Location: frontend/pages/login.js<br><br>import { useState } from "react";<br>import axios from "axios";<br><br>export default function Login() {<br>  const [email, setEmail] = useState("");<br>  const [password, setPassword] = useState("");<br>  const [message, setMessage] = useState("");<br><br>  const handleLogin = async (e) => {<br>    e.preventDefault();<br>    try {<br>      const res = await axios.post("http://localhost:8000/auth/login", { email, password });<br>      localStorage.setItem("token", res.data.access_token);<br>      setMessage("Login Successful!");<br>    } catch (error) {<br>      setMessage("Invalid Credentials!");<br>    }<br>  };<br><br>  return (<br>    <div className="flex justify-center items-center h-screen bg-gray-100"><br>      <div className="bg-white p-6 rounded-lg shadow-md w-96"><br>        <h2 className="text-2xl font-bold text-center">Login</h2><br>        <form onSubmit={handleLogin} className="mt-4"><br>          <input type="email" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} className="w-full p-2 border rounded-md mb-3" /><br>          <input type="password" placeholder="Password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full p-2 border rounded-md mb-3" /><br>          <button type="submit" className="w-full bg-blue-500 text-white py-2 rounded-md">Login</button><br>        </form><br>        {message && <p className="mt-2 text-center text-red-500">{message}</p>}<br>      </div><br>    </div><br>  );<br>}<br><br><br>---<br><br>4️⃣ auth.js (Token Management)<br><br>📍 Location: frontend/utils/auth.js<br><br>export const getToken = () => {<br>  return localStorage.getItem("token");<br>};<br><br>export const isAuthenticated = () => {<br>  return !!getToken();<br>};<br><br>export const logout = () => {<br>  localStorage.removeItem("token");<br>};<br><br><br>---<br><br>✅ How to Run?<br><br>1️⃣ Run Backend (FastAPI)<br><br>cd backend<br>uvicorn main:app --reload<br><br><br>---<br><br>2️⃣ Run Frontend (Next.js)<br><br>cd frontend<br>npm run dev<br><br><br>---<br><br>Bhai, yeh pura login system setup hai Next.js + Tailwind CSS (Frontend) & FastAPI + MongoDB (Backend) ke saath.<br><br>Agar koi dikkat aaye toh batao!<br><br>

# 📊 GitHub Stats:
![](https://github-readme-stats.vercel.app/api?username=Bb&theme=dark&hide_border=false&include_all_commits=false&count_private=false)<br/>
![](https://nirzak-streak-stats.vercel.app/?user=Bb&theme=dark&hide_border=false)<br/>
![](https://github-readme-stats.vercel.app/api/top-langs/?username=Bb&theme=dark&hide_border=false&include_all_commits=false&count_private=false&layout=compact)

---
[![](https://visitcount.itsvg.in/api?id=Bb&icon=0&color=0)](https://visitcount.itsvg.in)

<!-- Proudly created with GPRM ( https://gprm.itsvg.in ) -->